{
  "name": "project-generator",
  "version": "1.0.0",
  "description": "Web-based dev environment with OpenCode terminal and project preview",
  "main": "server.js",
  "scripts": {
    "start": "./start.sh",
    "dev": "./start.sh",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/darwin-*/spawn-helper 2>/dev/null || true"
  },
  "dependencies": {
    "discord.js": "^14.25.1",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "node-pty": "^1.1.0",
    "telegraf": "^4.16.3",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "playwright": "^1.58.0"
  }
}
