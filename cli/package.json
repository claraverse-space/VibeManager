{
  "name": "@vibemanager/cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "vibemanager": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "build:binary": "bun build src/index.ts --compile --outfile dist/vibemanager"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  }
}
